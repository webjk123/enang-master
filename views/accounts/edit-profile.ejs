<%layout('layout') -%>
<div class="row">
<div class="col-lg-6 col-md-offset-3">
<% if (message.length > 0) { %>
  <div class="alert alert-danger alert-dismissible">
  <button type="button" class="close" data-dismmiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
  <%= message %>
</div>
<% } %>

    <form method="post">

        <input class="span3" name="name" placeholder="Name" value="<%= user.profile.name %>" type="text">
        <input class="span3" name="address" placeholder="Address" value="<%= user.address %>" type="text">
        <button class="btn btn-warning" type="submit">Edit for WebApp</button>
    </form>

</div>
</div>
